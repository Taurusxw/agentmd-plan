# 2026-08-20 Round 002: Release 30.0.0 Governance

## Status

completed — candidate validation, two-repository publication, maintainer installation, tag, GitHub Release, and final publication verification are recorded.

## Goal

Implement, validate, install, and publish the `v30.0.0` governance release across the owner and Skills distribution repositories while keeping every Nature asset and the consumed HANDOFF frozen.

## Frozen Completion Contract

- Record `30.0.0` as the project target because governance-sensitive `seer-codex-rules` routing and the multi-Agent work model change future task behavior.
- Describe conditional routing: matching Skills still load normally, while `seer-codex-rules` loads only for rules/`AGENTS.md`, version/progress/documentation governance, release/migration/global sync, architecture drift, Goal/acceptance expansion, or multi-Agent work. Ordinary L1/L2 and single-file work stays outside this trigger.
- Preserve net-benefit, exclusive-write, effective-access, and compact-result boundaries for delegation; do not dispatch merely to use capacity.
- Preserve the zero-hash evidence boundary; create integrity metadata only once in the final named synchronization/recovery stage.
- Optimize only the five approved self-owned Seer Skills in an isolated distribution-repository worktree: `seer-capture`, `seer-project-space-health`, `seer-math-exam`, `seer-mathbook`, and `seer-prepare-open-source-release`.
- Do not install, sync, tag, push, or publish until the frozen candidate gates pass. After they pass, execute the already authorized two-repository publication and maintainer installation once; never repeat the completed 31-child stress test.
- Do not edit `docs/HANDOFF.md`. All `nature-*` source, version, test, installed copy, and `.agents` duplicate-copy targets are frozen; do not implement, validate, install, version, or clean them up in this round. Nature observations remain read-only future-audit candidates only.

## Baseline

- 20/20 routing prompts ran read-only with `gpt-5.6-sol`: total input `4,031,088`; median `168,567`.
- Nine scenarios loaded `seer-codex-rules`; no actual subagent dispatches and no hash commands occurred.
- Ordinary L1/L2/single-file prompts loaded governance in this baseline. The candidate changes that behavior; the baseline is observational evidence, not final candidate acceptance.
- Nature scenarios 16–20 are read-only observations only. They must not be presented as remediated, fixed, changed, or accepted by this release.

## Key Decisions

- Use `30.0.0` as a MAJOR target because conditional governance routing and the multi-Agent work model are behavioral-model changes.
- Treat the source repository and separate distribution repository as the two independently evidenced release targets; maintainer-environment installation is separately authorized and evidenced. No target is complete until the release owner records it.
- Keep only latest versioned assets in the local checkout after authorized release cleanup; preserve Git/GitHub history.
- Keep third-party Skills read-only. Disable candidates require a separate audit and explicit approval; no candidate is approved or changed here. All `nature-*` assets, including `.agents` duplicates, remain frozen rather than a disable or cleanup action.
- Keep the five Seer edits outside live installation until their focused validators and the integrated candidate gate pass; publish the distribution repository separately and without a GitHub Release.

## Planned Validation

- Review candidate version and documentation links/paths.
- Validate the governance Skill and all five modified Seer Skills from their source worktrees.
- Run only the frozen candidate checks selected by the release owner after integrated source changes exist.
- Record any resulting evidence in `docs/progress/releases/v30.0.0/ACCEPTANCE.md`; do not reuse `v29.1.0` publication evidence as `v30.0.0` proof.

## Candidate Evidence

- The final governance candidate passed `quick_validate.py`, Python compilation, the full 24-test shared unit suite, strict size measurement, static routing validation, and a non-live strict guardrail with state/copy comparison disabled.
- The five modified Seer Skills had already passed 5/5 focused `quick_validate.py` checks; `seer-mathbook` also passed its 4/4 routing tests. Their source did not change after that evidence.
- A temporary candidate `CODEX_HOME` preserved the current Skill/plugin set while replacing only the 30.0.0 global rule, `seer-codex-rules`, and the five approved Seer Skills. It did not modify live assets.
- Candidate scenarios 1–15 passed semantically under `gpt-5.6-sol` and read-only sandbox: 15/15 exit success, seven governance loads, zero subagent dispatches, and zero hash commands. The deliberately small read-only packet in scenario 7 stayed serial because its coordination cost exceeded benefit.
- Against the same 1–15 baseline, total input fell from `2,700,004` to `2,287,102` and median input from `169,355` to `133,725`. The ordinary cohort (1, 2, 3, 6, 11, 12) fell from `695,035` to `324,423` total input and from `153,779.5` to `60,810` median input (53.3% and 60.5% reductions).
- Scenarios 16–20 were not rerun because the user froze Nature source, tests, installation, validation, and duplicate cleanup. Their old read-only baseline remains observation only and is excluded from the 30.0.0 score.
- The first recovery-snapshot attempt stopped before writing because legacy snapshot code still required a persistent SHA256 line in the public rule inventory. The contract was corrected so the inventory anchors the version while the named recovery stage computes its own current digest; the affected shared suite then passed 25/25.
- The five-Seer distribution commit `38cd851` was pushed to `skills-manager-backup` `main` without a Release, then fast-forwarded into the canonical local source while preserving the user's unrelated `seer-codex-rules` deletions.
- A private pre-30.0.0 rollback backup was created. The 30.0.0 global rule, Downloads copy, governance Skill, and five approved Seer Skills are installed. The final strict live/state/global-copy guardrail passed with 26 Skill files and no mismatches.
- Only the byte-identical non-Nature `.agents/skills/researchwrite` duplicate was removed; the canonical copy remains available. Every Nature duplicate remains untouched.

## Rollback

Restore the previous accepted source-release and/or maintainer-environment state independently. Do not infer that rolling back one target rolls back the other; rerun the checks affected by that target.

## Publication

- Skills distribution: `skills-manager-backup/main@38cd851`, pushed without a Release.
- Source release commit: `4b4a2d7`; annotated tag: `v30.0.0`.
- GitHub Release: <https://github.com/Taurusxw/agentmd-plan/releases/tag/v30.0.0>; public, non-draft, non-prerelease, and latest at verification time.
- Local latest-only state keeps only tag `v30.0.0`; remote history remains intact.

## Risks And Follow-Up

- New live rules apply to new tasks or explicit host reloads; this task does not assume hot reload.
- `docs/HANDOFF.md` remains frozen and untouched.
